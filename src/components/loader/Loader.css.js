const styles = theme => ({
  loaderWrapper: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    padding: theme.spacing.unit * 2
  },
  progress: {
    margin: theme.spacing.unit * 2,
  },
  text: {
    padding: theme.spacing.unit * 2
  }
});

export {styles};
